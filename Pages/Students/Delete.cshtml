@page "{id:int}"
@model OnlineCourseManagementPortal.Pages.Students.DeleteModel
@{
    ViewData["Title"] = "Delete Student";
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card">
            <div class="card-header bg-danger text-white">
                <h3><i class="fas fa-trash"></i> Delete Student</h3>
            </div>
            <div class="card-body">
                <div class="alert alert-warning">
                    <h5>Are you sure you want to delete this student?</h5>
                    <p class="mb-0">This action will also delete all enrollments for this student.</p>
                </div>

                <dl class="row">
                    <dt class="col-sm-4">Name:</dt>
                    <dd class="col-sm-8">@Model.Student.FullName</dd>

                    <dt class="col-sm-4">Email:</dt>
                    <dd class="col-sm-8">@Model.Student.Email</dd>

                    <dt class="col-sm-4">Student ID:</dt>
                    <dd class="col-sm-8">@Model.Student.Id</dd>
                </dl>

                <form method="post">
                    <input type="hidden" asp-for="Student.Id" />
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a asp-page="./Index" class="btn btn-secondary me-md-2">Cancel</a>
                        <button type="submit" class="btn btn-danger">Delete Student</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>